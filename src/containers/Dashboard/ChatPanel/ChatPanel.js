import React, { Component } from "react";

let tempStyle = {
  border: "2px solid red",
  backgroundColor: "#ccc"
};

class ChatPanel extends Component {
  render() {
    return <div style={tempStyle}>Chat Panel</div>;
  }
}

export default ChatPanel;
